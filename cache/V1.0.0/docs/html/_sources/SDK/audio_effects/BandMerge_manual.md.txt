# 频带合并（Band Merge）

## 功能介绍
频带合并（将多路数据相加）

## 参数介绍
* **ByPass**：ByPass使能后，只输出Band0输入端口的数据
* **Band0 Gain**：频带0的输入增益，单位dB
* **Band1 Gain**：频带1的输入增益，单位dB
* **Band2 Gain**：频带2的输入增益，单位dB

## 注意事项
多个频带的数据输入长度必须相同，播放同步、带拼拆包的算法处理（空间音效v3.0.0、LLNS、LLNS_DNS等）可能会改变数据帧长，需要注意。